public class Solution {
    public int max(int n, int m, int k) {
        return Math.max(n, Math.max(m, k));
    }

    public int min(int n, int m, int k) {
        return Math.min(n, Math.min(m, k));
    }
}